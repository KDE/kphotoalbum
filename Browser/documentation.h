/**
  \namespace Browser
  \brief The component that allows you to narrow down the image set

  The GUI component of the browser is the BrowserWidget. This widget
  contains internally a QTreeView and a QListView (set up to
  IconMode). These two views are populated by instances of QAbstractItemModel's.

**/
